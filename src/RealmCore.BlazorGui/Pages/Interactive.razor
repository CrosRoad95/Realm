@page "/realmUi/interactive"
@using RealmCore.Server.Modules.Elements
@inherits RealmGuiComponentBase<InteractiveGui>;

Foo=@PlayerGui.Foo
Hello @(((RealmPlayer)PlayerGui.Player).Name)

@code{
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            PlayerGui.FooChanged += () =>
            {
                InvokeAsync(StateHasChanged);
            };
        }
    }
}